# Domain Docs

- **Layout:** Single-context
- **CONTEXT.md:** Repo root
- **ADRs:** `docs/adr/`

## Consumer Rules

Skills that read domain docs:

- `improve-codebase-architecture` — reads `CONTEXT.md` for domain language and `docs/adr/` for past decisions
- `diagnosing-bugs` — reads `CONTEXT.md` to understand domain constraints
- `tdd` — reads `CONTEXT.md` to understand domain terminology
